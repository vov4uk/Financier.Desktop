<UserControl x:Class="Financier.Desktop.Views.CurrenciesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Financier.Desktop.ViewModel"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <local:BindingProxy x:Key="Proxy" Data="{Binding}" />
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <DataGrid Margin="5"
                  ItemsSource="{Binding Path=Entities}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  FontSize="14"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{Binding Data.LocalizationManager.title, Source={StaticResource Proxy}}" Binding="{Binding Path=Title}"/>
                <DataGridTextColumn Header="{Binding Data.LocalizationManager.name, Source={StaticResource Proxy}}" Binding="{Binding Path=Name}"/>
                <DataGridTextColumn Header="{Binding Data.LocalizationManager.symbol, Source={StaticResource Proxy}}" Binding="{Binding Path=Symbol}"/>
                <DataGridTextColumn Header="{Binding Data.LocalizationManager.is_default, Source={StaticResource Proxy}}" Binding="{Binding Path=IsDefault}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
