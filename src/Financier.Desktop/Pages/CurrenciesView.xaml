<UserControl x:Class="Financier.Desktop.Views.CurrenciesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:loc="clr-namespace:Financier.Common.Localization;assembly=Financier.Common"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid Margin="5"
                  ItemsSource="{Binding Path=Entities}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  FontSize="14"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{loc:Translate Key=title}" Binding="{Binding Path=Title}"/>
                <DataGridTextColumn Header="{loc:Translate Key=name}" Binding="{Binding Path=Name}"/>
                <DataGridTextColumn Header="{loc:Translate Key=symbol}" Binding="{Binding Path=Symbol}"/>
                <DataGridTextColumn Header="{loc:Translate Key=is_default}" Binding="{Binding Path=IsDefault}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
