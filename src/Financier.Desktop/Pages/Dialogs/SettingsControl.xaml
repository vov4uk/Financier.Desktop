<UserControl x:Class="Financier.Desktop.Pages.Dialogs.SettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Financier.Desktop.Pages.Dialogs"
             xmlns:vm="clr-namespace:Financier.Desktop.ViewModel"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=local:SettingsVM, IsDesignTimeCreatable=True}"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <vm:BindingProxy x:Key="Proxy" Data="{Binding}" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TabControl TabStripPlacement="Left" 
                    Margin="0, 0, 0, 10"
                    Grid.Row="0">
            <TabItem Name="general" Header="{Binding Data.LocalizationManager.general, Source={StaticResource Proxy}}" IsSelected="True">
                <TabItem.Content>
                    <StackPanel Margin="5">
                        <CheckBox Margin="5" Content="{Binding Data.LocalizationManager.check_for_updates_on_start, Source={StaticResource Proxy}}" IsChecked="{Binding Entity.General.CheckForUpdatesOnStart, Mode=TwoWay}"/>
                    </StackPanel>
                </TabItem.Content>
            </TabItem>
            <TabItem Name="fontweight" Header="{Binding Data.LocalizationManager.exchange_rates, Source={StaticResource Proxy}}">
                <TabItem.Content>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <GroupBox Grid.Row="0" Grid.Column="0" Margin="5" Header="{Binding Data.LocalizationManager.providers, Source={StaticResource Proxy}}">
                            <StackPanel>
                                <RadioButton GroupName="Providers" Grid.Row="0" Margin="5" Content="openexchangerates.org" IsChecked="{Binding IsOpenExchangeRatesProviderSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                <RadioButton GroupName="Providers" Grid.Row="0" Margin="5" Content="freecurrencyrates.com" IsChecked="{Binding IsFreeCurrencyRatesProviderSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                <RadioButton GroupName="Providers" Grid.Row="0" Margin="5" Content="monobank.ua" IsChecked="{Binding IsMonobankProviderSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                        </GroupBox>
                        <GroupBox Grid.Row="1"
                                  Grid.Column="0"
                                  Margin="5"
                                  Header="{Binding Data.LocalizationManager.app_id, Source={StaticResource Proxy}}"
                                  Visibility="{Binding IsOpenExchangeRatesProviderSelected, Converter={StaticResource BoolToVis}, UpdateSourceTrigger=PropertyChanged}">
                            <TextBox Margin="5" Text="{Binding Entity.ExchangeRates.OpenExchangeRatesProviderAppId, Mode=TwoWay}"
                                     />
                        </GroupBox>
                        <CheckBox Grid.Row="2" Grid.Column="0" Margin="5" Content="{Binding Data.LocalizationManager.update_exchange_rates_on_start, Source={StaticResource Proxy}}"
                                  IsChecked="{Binding Entity.ExchangeRates.UpdateOnStart, Mode=TwoWay}"/>
                    </Grid>
                </TabItem.Content>
            </TabItem>
        </TabControl>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Margin="2"
                    Command="{Binding SaveCommand}"
                    Content="{Binding Data.LocalizationManager.save, Source={StaticResource Proxy}}" />
            <Button Grid.Column="1" Margin="2"
                    Command="{Binding CancelCommand}"
                    Content="{Binding Data.LocalizationManager.cancel, Source={StaticResource Proxy}}" />
        </Grid>
    </Grid>
</UserControl>
