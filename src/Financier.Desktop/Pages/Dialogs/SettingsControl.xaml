<UserControl x:Class="Financier.Desktop.Pages.Dialogs.SettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Financier.Desktop.Pages.Dialogs"
             mc:Ignorable="d"
             xmlns:loc="clr-namespace:Financier.Common.Localization;assembly=Financier.Common"
             xmlns:ent="clr-namespace:Financier.Common.Entities;assembly=Financier.Common"
             d:DataContext="{d:DesignInstance Type=local:SettingsVM, IsDesignTimeCreatable=True}"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TabControl TabStripPlacement="Left" 
                    Margin="0, 0, 0, 10"
                    Grid.Row="0">
            <TabItem Name="general" Header="{loc:Translate Key=general}" IsSelected="True">
                <TabItem.Content>
                    <StackPanel Margin="5">
                        <GroupBox Grid.Row="0" Grid.Column="0" Margin="5" Header="{loc:Translate Key=language}">
                            <ComboBox Width="180"
                                      Margin="5,0,0,0"
                                      HorizontalAlignment="Left"
                                      ItemsSource="{loc:EnumBinding {x:Type loc:Language}}"
                                      SelectedItem="{Binding Entity.General.Language, Mode=TwoWay}"/>
                        </GroupBox>

                        <CheckBox Margin="5" Content="{loc:Translate Key=check_for_updates_on_start}" IsChecked="{Binding Entity.General.CheckForUpdatesOnStart, Mode=TwoWay}"/>
                    </StackPanel>
                </TabItem.Content>
            </TabItem>
            <TabItem Name="fontweight" Header="{loc:Translate Key=exchange_rates}">
                <TabItem.Content>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <GroupBox Grid.Row="0" Grid.Column="0" Margin="5" Header="{loc:Translate Key=providers}">
                            <ComboBox Width="200" Margin="5,0,0,0"
                                      HorizontalAlignment="Left"
                                      ItemsSource="{loc:EnumBinding {x:Type ent:ExchangeRatesProviders}}"
                                      SelectedItem="{Binding SelectedProvider, Mode=TwoWay}"/>
                        </GroupBox>
                        <GroupBox Grid.Row="1"
                                  Grid.Column="0"
                                  Margin="5"
                                  Header="{loc:Translate Key=app_id}"
                                  Visibility="{Binding IsOpenExchangeRatesProviderSelected, Converter={StaticResource BoolToVis}, UpdateSourceTrigger=PropertyChanged}">
                            <TextBox Margin="5" Text="{Binding Entity.ExchangeRates.OpenExchangeRatesProviderAppId, Mode=TwoWay}"
                                     />
                        </GroupBox>
                        <CheckBox Grid.Row="2" Grid.Column="0" Margin="5" Content="{loc:Translate Key=update_exchange_rates_on_start}"
                                  IsChecked="{Binding Entity.ExchangeRates.UpdateOnStart, Mode=TwoWay}"/>
                    </Grid>
                </TabItem.Content>
            </TabItem>
        </TabControl>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Margin="2"
                    Command="{Binding SaveCommand}"
                    Content="{loc:Translate Key=save}" />
            <Button Grid.Column="1" Margin="2"
                    Command="{Binding CancelCommand}"
                    Content="{loc:Translate Key=cancel}" />
        </Grid>
    </Grid>
</UserControl>
