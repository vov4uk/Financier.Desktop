<UserControl x:Class="Financier.Desktop.Wizards.RecipesWizard.View.Page1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Financier.Desktop.Wizards.RecipesWizard.View"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:vm="clr-namespace:Financier.Desktop.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450"
             d:DesignWidth="800">
    <UserControl.Resources>
        <vm:BindingProxy x:Key="Proxy" Data="{Binding}" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="34"/>
            <RowDefinition Height="34"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{Binding Data.LocalizationManager.recipes_instructions_line1, Source={StaticResource Proxy}}"/>
        <Grid Grid.Column="0"
              Grid.Row="1"
              Margin="10 10 0 0"
              Height="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0"
                    Width="65"
                    Margin="2"
                    HorizontalAlignment="Stretch"
                    Content="{Binding Data.LocalizationManager.paste, Source={StaticResource Proxy}}"
                    ToolTip="{Binding Data.LocalizationManager.paste_tooltip, Source={StaticResource Proxy}}"
                    Command="ApplicationCommands.Paste"
                    CommandTarget="{Binding ElementName=_richTextBox}"
                />
            <Button Grid.Column="1"
                    Width="65"
                    Margin="2"
                    HorizontalAlignment="Stretch"
                    Content="{Binding Data.LocalizationManager.highlight, Source={StaticResource Proxy}}"
                    ToolTip="{Binding Data.LocalizationManager.highlight_tooltip, Source={StaticResource Proxy}}"
                    Command="{Binding Path=HighlightCommand}"
                    CommandParameter="{Binding ElementName=_richTextBox}"
                />
            <TextBlock Grid.Column="2"
                       HorizontalAlignment="Right"
                       Text="{Binding FormattedTotal}" />
        </Grid>

        <xctk:RichTextBox x:Name="_richTextBox"
                          Grid.Row="2"
                          Margin="10"
                          BorderBrush="Gray"
                          Padding="10"
                          Text="{Binding Text, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, NotifyOnSourceUpdated=True}"
                          ScrollViewer.VerticalScrollBarVisibility="Auto">
            <xctk:RichTextBox.TextFormatter>
                <local:RecipesFormatter />
            </xctk:RichTextBox.TextFormatter>
        </xctk:RichTextBox>
    </Grid>
</UserControl>
