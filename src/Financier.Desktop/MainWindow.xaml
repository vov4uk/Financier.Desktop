<RibbonWindow  x:Class="Financier.Desktop.MainWindow"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:data="clr-namespace:Financier.Common.Model;assembly=Financier.Common"
               xmlns:converters="clr-namespace:Financier.Converters;assembly=Financier.Common"
               xmlns:reports="clr-namespace:Financier.Reports;assembly=Financier.Reports"
               xmlns:loc="clr-namespace:Financier.Common.Localization;assembly=Financier.Common"
               Icon="pack://application:,,,/Images/ic_launcher.png"
               xmlns:ViewModel="clr-namespace:Financier.Desktop.ViewModel"
               xmlns:View="clr-namespace:Financier.Desktop.Views"
               xmlns:Wizard="clr-namespace:Financier.Desktop.Wizards"
               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
               xmlns:s="clr-namespace:System;assembly=mscorlib"
               AllowDrop="True"
               Loaded="RibbonWindow_Loaded">
    <Window.Resources>
        <ResourceDictionary>
            <DataTemplate DataType="{x:Type ViewModel:AccountsVM}">
                <View:AccountsView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type ViewModel:BlotterVM}">
                <View:BlotterView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type ViewModel:CategoriesVM}">
                <View:CategoriesView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type ViewModel:CurrenciesVM}">
                <View:CurrenciesView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type ViewModel:ExchangeRatesVM}">
                <View:ExchangeRatesView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type ViewModel:LocationsVM}">
                <View:LocationsView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type ViewModel:PayeesVM}">
                <View:TagView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type ViewModel:ProjectsVM}">
                <View:TagView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type reports:ReportsControlVM}">
                <reports:ReportsControl/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type ViewModel:RulesVM}">
                <View:RulesView/>
            </DataTemplate>
            <x:Type x:Key="AccountType" TypeName="data:AccountModel" />
            <x:Type x:Key="CategoryType" TypeName="data:CategoryTreeModel" />
            <x:Type x:Key="ProjectType" TypeName="data:ProjectModel" />
            <x:Type x:Key="PayeeType" TypeName="data:PayeeModel" />
            <x:Type x:Key="LocationType" TypeName="data:LocationModel" />
            <x:Type x:Key="CurrencyType" TypeName="data:CurrencyModel" />
            <x:Type x:Key="CurrencyExchangeRateType" TypeName="data:ExchangeRateModel" />
            <x:Type x:Key="TransactionsViewType" TypeName="data:BlotterModel" />
            <x:Type x:Key="ReportsControl" TypeName="reports:ReportsControlVM" />
            <x:Type x:Key="RulesType" TypeName="data:RuleModel" />
            <s:Boolean x:Key="True">True</s:Boolean>
            <converters:StringEmptyToVisibilityConverter x:Key="StringToVisibilityConverter" />
            <BooleanToVisibilityConverter x:Key="BoolToVis" />
            <converters:InverseBooleanConverter x:Key="InverseBool" />
            <ContextMenu x:Key="HiddenContextMenu" Visibility="Hidden"/>
            <Style TargetType="Ribbon">
                <Setter Property="Ribbon.ContextMenu" Value="{StaticResource HiddenContextMenu}"/>
            </Style>
            <Style TargetType="RibbonTab">
                <Setter Property="RibbonTab.ContextMenu" Value="{StaticResource HiddenContextMenu}"/>
            </Style>
            <Style TargetType="RibbonGroup">
                <Setter Property="RibbonGroup.ContextMenu" Value="{StaticResource HiddenContextMenu}"/>
            </Style>
            <Style TargetType="RibbonButton">
                <Setter Property="RibbonButton.ContextMenu" Value="{StaticResource HiddenContextMenu}"/>
            </Style>
            <Style TargetType="RibbonRadioButton">
                <Setter Property="RibbonRadioButton.ContextMenu" Value="{StaticResource HiddenContextMenu}"/>
            </Style>
            <Style TargetType="RibbonTextBox">
                <Setter Property="RibbonTextBox.ContextMenu" Value="{StaticResource HiddenContextMenu}"/>
            </Style>
            <Style TargetType="RibbonComboBox">
                <Setter Property="RibbonComboBox.ContextMenu" Value="{StaticResource HiddenContextMenu}"/>
            </Style>
            <Style TargetType="RibbonApplicationMenuItem">
                <Setter Property="RibbonApplicationMenuItem.ContextMenu" Value="{StaticResource HiddenContextMenu}"/>
            </Style>
            <Style TargetType="RibbonGalleryCategory">
                <Setter Property="RibbonGalleryCategory.ContextMenu" Value="{StaticResource HiddenContextMenu}"/>
            </Style>
            <Style TargetType="RibbonGallery">
                <Setter Property="RibbonGallery.ContextMenu" Value="{StaticResource HiddenContextMenu}"/>
            </Style>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Assets/Generic.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid AllowDrop="True"
          IsEnabled="{Binding Path=IsLoading, Converter={StaticResource InverseBool}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Ribbon x:Name="FinancierDesktop"
                SelectedIndex="0">
            <Ribbon.HelpPaneContent>
                <Label Content="{Binding Path=OpenBackupPath}" />
            </Ribbon.HelpPaneContent>

            <Ribbon.ApplicationMenu>
                <RibbonApplicationMenu ContextMenu="{x:Null}"
                                       Background="#858585"
                                       BorderBrush="#858585">
                    <RibbonApplicationMenuItem Header="{loc:Translate Key=openBackup}"
                                               Command="{Binding Path=OpenBackupCommand}"
                                               ImageSource="{StaticResource ResourceKey=IconFileArrowUp}" />
                    <RibbonApplicationMenuItem Header="{loc:Translate Key=saveBackup}"
                                               Command="{Binding Path=SaveBackupCommand}"
                                               ImageSource="{StaticResource ResourceKey=IconFileArrowDown}" />
                    <RibbonApplicationMenuItem Header="{loc:Translate Key=saveAsDb}"
                                               Command="{Binding Path=SaveBackupAsDbCommand}"
                                               ImageSource="{StaticResource ResourceKey=IconDatabase}" />
                    <RibbonApplicationMenuItem Header="{loc:Translate Key=setDefaultDirectory}"
                                               Click="RibbonApplicationMenuItem_Click"
                                               ImageSource="{StaticResource ResourceKey=IconDropboxBrands}" />
                    <RibbonApplicationMenuItem Header="{loc:Translate Key=settings}"
                                               Command="{Binding Path=SettingsCommand}"
                                               ImageSource="{StaticResource ResourceKey=IconGear}" />
                    <RibbonApplicationMenu.FooterPaneContent>
                        <DockPanel LastChildFill="False">
                            <RibbonButton Click="Exit_Click"
                                          SmallImageSource="{StaticResource IconArrowRightToBracket}"
                                          Label="{loc:Translate Key=exit}"
                                          ToolTipTitle="Exit"
                                          KeyTip="X"
                                          DockPanel.Dock="Right"
                                          Margin="2"
                                          BorderBrush="#858585" />
                        </DockPanel>
                    </RibbonApplicationMenu.FooterPaneContent>
                    <RibbonApplicationMenu.AuxiliaryPaneContent>
                        <RibbonGallery CanUserFilter="False"
                                       ScrollViewer.VerticalScrollBarVisibility="Auto">
                            <RibbonGalleryCategory Header="{loc:Translate Key=defaultBackupDirectory}"
                                                   Background="Transparent">
                                <RibbonGalleryCategory.Items>
                                    <RibbonGalleryItem IsEnabled="False">
                                        <Label Content="{Binding Path=DefaultBackupDirectory}"/>
                                    </RibbonGalleryItem>
                                </RibbonGalleryCategory.Items>
                                <RibbonGalleryCategory.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Vertical"
                                                    ToolTip="{Binding Path=DefaultBackupDirectory}"
                                                    HorizontalAlignment="Left"
                                                    CanHorizontallyScroll="False"
                                                    IsItemsHost="True"/>
                                    </ItemsPanelTemplate>
                                </RibbonGalleryCategory.ItemsPanel>
                            </RibbonGalleryCategory>
                        </RibbonGallery>
                    </RibbonApplicationMenu.AuxiliaryPaneContent>
                </RibbonApplicationMenu>
            </Ribbon.ApplicationMenu>

            <RibbonTab Header="{loc:Translate Key=main}"
                       Selector.IsSelected="True">
                <RibbonGroup Header="{loc:Translate Key=entities}" ContextMenu="{x:Null}">
                    <RibbonButton LargeImageSource="{StaticResource ResourceKey=IconWallet}"
                                  Label="{loc:Translate Key=accounts}"
                                  Command="{Binding Path=MenuNavigateCommand}"
                                  CommandParameter="{StaticResource AccountType}" />
                    <RibbonButton SmallImageSource="{StaticResource ResourceKey=IconFolderTree}"
                                  Label="{loc:Translate Key=categories}" Command="{Binding Path=MenuNavigateCommand}"
                                  CommandParameter="{StaticResource CategoryType}" />
                    <RibbonButton SmallImageSource="{StaticResource ResourceKey=IconListCheck}"
                                  Label="{loc:Translate Key=projects}" Command="{Binding Path=MenuNavigateCommand}"
                                  CommandParameter="{StaticResource ProjectType}" />
                    <RibbonButton SmallImageSource="{StaticResource ResourceKey=IconAddressBook}"
                                  Label="{loc:Translate Key=payees}" Command="{Binding Path=MenuNavigateCommand}"
                                  CommandParameter="{StaticResource PayeeType}" />
                    <RibbonButton SmallImageSource="{StaticResource ResourceKey=IconMap}"
                                  Label="{loc:Translate Key=locations}" Command="{Binding Path=MenuNavigateCommand}"
                                  CommandParameter="{StaticResource LocationType}" />
                    <RibbonButton SmallImageSource="{StaticResource ResourceKey=IconDollarSign}"
                                  Label="{loc:Translate Key=currencies}" Command="{Binding Path=MenuNavigateCommand}"
                                  CommandParameter="{StaticResource CurrencyType}" />
                    <RibbonButton SmallImageSource="{StaticResource ResourceKey=IconArrowTrendUp}"
                                  Label="{loc:Translate Key=exchange_rates}"
                                  Command="{Binding Path=MenuNavigateCommand}"
                                  CommandParameter="{StaticResource CurrencyExchangeRateType}" />
                </RibbonGroup>

                <RibbonGroup x:Name="Blotter"
                             Header="{loc:Translate Key=blotter}">
                    <RibbonButton LargeImageSource="{StaticResource ResourceKey=IconReceipt}"
                                  Label="{loc:Translate Key=blotter}"
                                  Command="{Binding Path=MenuNavigateCommand}"
                                  CommandParameter="{StaticResource TransactionsViewType}" />
                    <!--<RibbonButton SmallImageSource="pack://application:,,,/Images/actionbar_filter_cancel.png" Label="Clear filter" />-->
                </RibbonGroup>

                <RibbonGroup x:Name="Reports"
                             Header="{loc:Translate Key=reports}">
                    <RibbonButton LargeImageSource="{StaticResource ResourceKey=IconChartBar}"
                                  Label="{loc:Translate Key=reports}"
                                  Command="{Binding Path=MenuNavigateCommand}"
                                  CommandParameter="{StaticResource ReportsControl}" />
                </RibbonGroup>
                <RibbonGroup x:Name="Rules"
                             Header="{loc:Translate Key=rules}">
                    <RibbonButton LargeImageSource="{StaticResource ResourceKey=IconBoltLightning}"
                                  Label="{loc:Translate Key=rules}"
                                  Command="{Binding Path=MenuNavigateCommand}"
                                  CommandParameter="{StaticResource RulesType}" />
                </RibbonGroup>
                <!--<RibbonGroup x:Name="Export" Header="Export" IsEnabled="False">
                    <RibbonButton LargeImageSource="pack://application:,,,/Images/actionbar_export.png"
                                  Label="Export to excel" />
                    <RibbonButton SmallImageSource="pack://application:,,,/Images/backup_file.png"
                                  Label="Export to csv" />
                </RibbonGroup>-->
                <RibbonGroup Header="{loc:Translate Key=import}"
                             IsEnabled="True">
                    <RibbonButton LargeImageSource="pack://application:,,,/Images/bank/monobank.png"
                                  Label="{loc:Translate Key=monobank_csv}"
                                  Command="{Binding Path=ImportCommand}"
                                  CommandParameter="{x:Static Wizard:WizardTypes.Monobank}" />
                    <RibbonMenuButton  Label="{loc:Translate Key=a_bank}"
                                       LargeImageSource="pack://application:,,,/Images/bank/abank.png" >

                        <RibbonMenuItem  Header="{loc:Translate Key=pdf}"
                                         Foreground="Red"
                                         Command="{Binding Path=ImportCommand}"
                                         CommandParameter="{x:Static Wizard:WizardTypes.ABank}" />
                        <RibbonMenuItem  Header="{loc:Translate Key=xlsx}"
                                         Foreground="Green"
                                         Command="{Binding Path=ImportCommand}"
                                         CommandParameter="{x:Static Wizard:WizardTypes.ABankExcel}" />
                    </RibbonMenuButton>

                    <RibbonButton LargeImageSource="pack://application:,,,/Images/bank/pumb.png"
                                  Label="{loc:Translate Key=pumb_pdf}"
                                  Command="{Binding Path=ImportCommand}"
                                  CommandParameter="{x:Static Wizard:WizardTypes.Pumb}" />
                    <RibbonButton LargeImageSource="pack://application:,,,/Images/bank/pireus.png"
                                  Label="{loc:Translate Key=pireus_pdf}"
                                  Command="{Binding Path=ImportCommand}"
                                  CommandParameter="{x:Static Wizard:WizardTypes.Pireus}" />
                    <RibbonButton LargeImageSource="pack://application:,,,/Images/bank/privat.png"
                                  Label="{loc:Translate Key=privat_xlsx}"
                                  Command="{Binding Path=ImportCommand}"
                                  CommandParameter="{x:Static Wizard:WizardTypes.Privat}" />
                    <RibbonButton LargeImageSource="pack://application:,,,/Images/bank/pko.png"
                                  Label="{loc:Translate Key=pko_pdf}"
                                  Command="{Binding Path=ImportCommand}"
                                  CommandParameter="{x:Static Wizard:WizardTypes.Pko}" />
                </RibbonGroup>

                <RibbonGroup x:Name="Transaction"
                             DataContext="{Binding}"
                             Visibility="{Binding Path=IsTransactionPageSelected, Converter={StaticResource BoolToVis}, Mode=OneWay}"
                             Header="{loc:Translate Key=transaction}">
                    <RibbonButton LargeImageSource="{StaticResource ResourceKey=IconPlus}"
                                  Label="{loc:Translate Key=add_expense}"
                                  Command="{Binding Path=Blotter.AddCommand}" />
                    <RibbonButton LargeImageSource="{StaticResource ResourceKey=IconArrowRightArrowLeft}"
                                  Label="{loc:Translate Key=add_transfer}"
                                  Command="{Binding Path=Blotter.AddTransferCommand}" />
                    <RibbonButton LargeImageSource="{StaticResource ResourceKey=IconTable}"
                                  Label="{loc:Translate Key=add_template}"
                                  Command="{Binding Path=Blotter.AddTemplateCommand}" />
                    <RibbonButton LargeImageSource="{StaticResource ResourceKey=IconPencil}"
                                  Label="{loc:Translate Key=edit}"
                                  Command="{Binding Path=Blotter.EditCommand}" />
                    <RibbonButton LargeImageSource="{StaticResource ResourceKey=IconCopy}"
                                  Label="{loc:Translate Key=duplicate}"
                                  Command="{Binding Path=Blotter.DuplicateCommand}" />
                    <RibbonButton LargeImageSource="{StaticResource ResourceKey=IconTrash}"
                                  Label="{loc:Translate Key=delete}"
                                  Command="{Binding Path=Blotter.DeleteCommand}" />
                    <RibbonButton LargeImageSource="{StaticResource ResourceKey=IconCircleInfo}"
                                  Label="{loc:Translate Key=info}"
                                  Command="{Binding Path=Blotter.InfoCommand}" />
                </RibbonGroup>

                <RibbonGroup Header="{loc:Translate Key=locations}"
                             Visibility="{Binding Path=IsLocationPageSelected, Converter={StaticResource BoolToVis}, Mode=OneWay}"
                             DataContext="{Binding}">
                    <RibbonButton LargeImageSource="{StaticResource ResourceKey=IconPlus}"
                                  Label="{loc:Translate Key=add}"
                                  Command="{Binding Path=Locations.AddCommand}" />
                    <RibbonButton LargeImageSource="{StaticResource ResourceKey=IconPencil}"
                                  Label="{loc:Translate Key=edit}"
                                  Command="{Binding Path=Locations.EditCommand}" />
                    <RibbonButton LargeImageSource="{StaticResource ResourceKey=IconTrash}"
                                  Label="{loc:Translate Key=delete}"
                                  IsEnabled="False"
                                  Command="{Binding Path=Locations.DeleteCommand}" />
                </RibbonGroup>

                <RibbonGroup Header="{loc:Translate Key=project}"
                             Selector.IsSelected="{Binding Path=IsProjectPageSelected, Mode=OneWay}"
                             Visibility="{Binding Path=IsProjectPageSelected, Converter={StaticResource BoolToVis}, Mode=OneWay}"
                             DataContext="{Binding}">
                    <RibbonButton LargeImageSource="{StaticResource ResourceKey=IconPlus}"
                                  Label="{loc:Translate Key=add}"
                                  Command="{Binding Path=Projects.AddCommand}" />
                    <RibbonButton LargeImageSource="{StaticResource ResourceKey=IconPencil}"
                                  Label="{loc:Translate Key=edit}"
                                  Command="{Binding Path=Projects.EditCommand}" />
                    <RibbonButton LargeImageSource="{StaticResource ResourceKey=IconTrash}"
                                  Label="{loc:Translate Key=delete}"
                                  IsEnabled="False"
                                  Command="{Binding Path=Projects.DeleteCommand}" />
                </RibbonGroup>

                <RibbonGroup Header="{loc:Translate Key=payee}"
                             Selector.IsSelected="{Binding Path=IsPayeePageSelected, Mode=OneWay}"
                             Visibility="{Binding Path=IsPayeePageSelected, Converter={StaticResource BoolToVis}, Mode=OneWay}"
                             DataContext="{Binding}">
                    <RibbonButton LargeImageSource="{StaticResource ResourceKey=IconPlus}"
                                  Label="{loc:Translate Key=add}"
                                  Command="{Binding Path=Payees.AddCommand}" />
                    <RibbonButton LargeImageSource="{StaticResource ResourceKey=IconPencil}"
                                  Label="{loc:Translate Key=edit}"
                                  Command="{Binding Path=Payees.EditCommand}" />
                    <RibbonButton LargeImageSource="{StaticResource ResourceKey=IconTrash}"
                                  Label="{loc:Translate Key=delete}"
                                  IsEnabled="False"
                                  Command="{Binding Path=Payees.DeleteCommand}" />
                </RibbonGroup>
                <RibbonGroup Header="{loc:Translate Key=rule}"
                             Selector.IsSelected="{Binding Path=IsRulesPageSelected, Mode=OneWay}"
                             Visibility="{Binding Path=IsRulesPageSelected, Converter={StaticResource BoolToVis}, Mode=OneWay}"
                             DataContext="{Binding}">
                    <RibbonButton LargeImageSource="{StaticResource ResourceKey=IconPlus}"
                                  Label="{loc:Translate Key=add}"
                                  Command="{Binding Path=Rules.AddCommand}" />
                    <RibbonButton LargeImageSource="{StaticResource ResourceKey=IconPencil}"
                                  Label="{loc:Translate Key=edit}"
                                  Command="{Binding Path=Rules.EditCommand}" />
                    <RibbonButton LargeImageSource="{StaticResource ResourceKey=IconTrash}"
                                  Label="{loc:Translate Key=delete}"
                                  Command="{Binding Path=Rules.DeleteCommand}" />
                </RibbonGroup>
                <RibbonGroup Header="{loc:Translate Key=exchange_rates}"
                             Selector.IsSelected="{Binding Path=IsExchangeRatesPageSelected, Mode=OneWay}"
                             Visibility="{Binding Path=IsExchangeRatesPageSelected, Converter={StaticResource BoolToVis}, Mode=OneWay}"
                             DataContext="{Binding}">
                    <RibbonButton LargeImageSource="{StaticResource ResourceKey=IconDownload}"
                                  Label="{loc:Translate Key=download}"
                                  Command="{Binding Path=RefreshExchangeRatesCommand}" />
                </RibbonGroup>
                <RibbonGroup Header="{loc:Translate Key=general}"
                         DataContext="{Binding}">
                    <RibbonButton LargeImageSource="{StaticResource ResourceKey=IconRefresh}"
                              Label="{loc:Translate Key=checkForUpdates}"
                              Command="{Binding Path=CheckForUpdateCommand}"
                              CommandParameter="{StaticResource True}" />
                </RibbonGroup>
            </RibbonTab>
        </Ribbon>
        <DockPanel Grid.Row="1">
            <ContentPresenter Content="{Binding Path=CurrentPage}"
                              DataContext="{Binding Path=CurrentPage}" />
        </DockPanel>
        <Grid Grid.Row="1"
              AllowDrop="True"
              Visibility="{Binding Path=OpenBackupPath, Converter={StaticResource StringToVisibilityConverter}}"
              Drop="Grid_Drop">
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                <Image Source="{StaticResource IconDragDrop}"
                       Height="256"
                       Width="256" />
                <TextBlock HorizontalAlignment="Center"
                           FontSize="14"
                           FontWeight="Bold"
                           Text="{loc:Translate Key=drag_and_drop}" />
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1"
              AllowDrop="True"
              Visibility="{Binding Path=IsLoading, Converter={StaticResource BoolToVis}}">
            <Image Height="64" Width="64" Source="{StaticResource IconRefresh}" RenderTransformOrigin="0.5,0.5">
                <Image.RenderTransform>
                    <RotateTransform x:Name="SpinTransform" Angle="0"/>
                </Image.RenderTransform>
                <Image.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard>
                            <Storyboard RepeatBehavior="Forever">
                                <DoubleAnimation Storyboard.TargetName="SpinTransform"
                                                 Storyboard.TargetProperty="Angle"
                                                 From="0" To="360"
                                                 Duration="0:0:1"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Image.Triggers>
            </Image>
        </Grid>
    </Grid>
</RibbonWindow >

